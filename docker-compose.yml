version: '3.8'

services:
  database:
    image: postgres:15-alpine
    container_name: intrabank_db
    environment:
      POSTGRES_USER: sa
      POSTGRES_PASSWORD: MinhaSenhaSuperSegura123!
      POSTGRES_DB: intrabank_financeiro
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
